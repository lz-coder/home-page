fetch ("./engines.json")
    .then((res) => res.json())
    .then((data) => {
        let searchEngine = document.querySelectorAll(".search-engine");
        for (i=0, i<=searchEngine.length,i++) {
            
        }
    });